pipeline {
    agent any
    stages {
        stage('Run shell') {
            steps {
                script {
                    sh 'docker --version'
                }
            }
        }
    }
}
