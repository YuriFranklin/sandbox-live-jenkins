pipeline {
    agent {
        label 'docker-agent'
    }
    stages {
        stage('Build') {
            steps {
                sh 'docker --version'
            }
        }
        stage('Test') {
            steps {
                sh 'docker run --rm alpine echo "Hello, Docker! 2"'
            }
        }
    }
}